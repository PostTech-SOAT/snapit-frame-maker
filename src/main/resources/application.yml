spring:
    application:
        name:
            application:
                name: snapit-frame-maker

    rabbitmq:
        host: ab6737414eea54b5fbbf7d8071ae75d1-1338972412.us-east-1.elb.amazonaws.com
        port: 5672
        username: hexqueue
        password: 1xo4r9VIU4ARLW3f
camel:
    component:
        aws2-s3:
            accessKey: ${AWS_ACCESS_KEY_ID}
            secretKey: ${AWS_SECRET_ACCESS_KEY}
            sessionToken: ${AWS_SESSION_TOKEN}
            region: ${AWS_REGION}
            # Parâmetros adicionais:
            useDefaultCredentialsProvider: false
        springboot:
            main-run-controller: true

management:
    server:
        port: 28083
    endpoint:
        health:
            probes:
                enabled: true
    health:
        livenessstate:
            enabled: true
        readinessstate:
            enabled: true
